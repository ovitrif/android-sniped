package com.sniped.ui.adapter

import com.xwray.groupie.Item
import com.xwray.groupie.ViewHolder

/**
 * Base item for recycler views.
 * Uses the Groupie library to abstract the boilerplate of multiple item view types.
 */
abstract class BaseAdapterItem : Item<ViewHolder>()
