package com.sniped.ui.adapter

import com.xwray.groupie.GroupAdapter
import com.xwray.groupie.ViewHolder

/**
 * Base adapter for recycler views.
 * Uses the Groupie library to eliminate the need of writing ViewHolder classes.
 */
abstract class BaseAdapter : GroupAdapter<ViewHolder>()
